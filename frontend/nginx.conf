server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index html/loginProfessor.html;

    location = / {
        return 302 /html/loginProfessor.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
